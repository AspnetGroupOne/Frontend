import React from 'react'

const ModalEventRules = ({ eventName, id, closeModal }) => {
  return (
    <>
      <button className='btn btn-pink' onClick={() => closeModal()}>X</button>
    
    
    
    
    
    </>
  )
}

export default ModalEventRules